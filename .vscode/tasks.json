{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "build",
            "type": "shell",
            "options": {
                "cwd": "${workspaceFolder}/wasm"
            },
            "command": "cargo",
            "args": [
                "build"
            ]
        }
    ]
}