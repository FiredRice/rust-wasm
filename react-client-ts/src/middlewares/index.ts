import react_middleware from './react';
import wasm_middleware from './wasm';
import antd_middleware from './antd';

export default [
    react_middleware,
    wasm_middleware,
    antd_middleware,
];